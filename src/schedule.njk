---
layout: page-layout.njk
title: Schedule
meta_title: '2017 Schedule :: Vermont Code Camp'
description: The Vermont Code Camp 2017 schedule included sessions on a variety of software topics, including programming, databases, web and more.
meta_description: The Vermont Code Camp 2017 schedule included sessions on a variety of software topics, including programming, databases, web and more.
---


<section id="content" class="event-2017">

    <h1>{{title}}</h1>
    
    <p> {{description}} 
        <em>Please read our <a href="/conduct">Code of Conduct</a> and <a href="/diversity">Diversity Statement</a>.</em>
    </p>

    <table class="schedule">

        <thead>
            <tr>
            {% for th in schedule.head %}
                <th scope="col" class="{{th.type}}">
                    {{ th.title }} 
                    {% if th.subtitle %}
                        <small class="room">{{ th.subtitle }}</small>
                    {% endif %}
                </th>
            {% endfor %}
            </tr>
        </thead>

        <tbody>
        {% for row in schedule.body %}

            <tr>
            {% for cell in row %} 
                {% if cell.type == "timespan" %}
                    <td id="{{ cell.timeSlug }}" class="{{ cell.type }}" scope="row">
                        {{ cell.title }}
                    </td>
                {% elif cell.type == "plenumSession" %}
                    <td colspan="5"> {{ cell.title }} </td>
                {% else %}
                    <td> 
                        <a href="{{ cell.title_link }}">{{ cell.title }}</a>
                        <small class="speaker">
                        {% for speaker in cell.speakers %}
                            <a href="{{ speaker.link }}">{{ speaker.name }}</a>
                        {% endfor %}
                        </small>
                    </td>
                {% endif %}
            {% endfor %} 
            </tr>

        {% endfor %}
        </tbody>

    </table>

</section>

